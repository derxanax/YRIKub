# <div align="center">🌟 YRIKub 🌟</div>

<div align="center">
  <img src="assets/img/logo.png" alt="YRIKub Logo" width="300" />
  <br>
  <p><b>Мощный двухкомпонентный Telegram-бот на TypeScript</b></p>
  
  ![TypeScript](https://img.shields.io/badge/TypeScript-%23007ACC.svg?style=for-the-badge&logo=typescript&logoColor=white)
  ![Telegram](https://img.shields.io/badge/Telegram-2CA5E0?style=for-the-badge&logo=telegram&logoColor=white)
  ![Node.js](https://img.shields.io/badge/Node.js-339933?style=for-the-badge&logo=nodedotjs&logoColor=white)
</div>

## 📑 Содержание

- [📋 Обзор](#-обзор)
- [🌟 Возможности](#-возможности)
- [⚙️ Архитектура](#️-архитектура)
- [🚀 Установка](#-установка)
- [🔧 Настройка](#-настройка)
- [▶️ Запуск](#️-запуск)
- [📝 Команды](#-команды)
- [📂 Модули](#-модули)
- [🛠️ Разработка](#️-разработка)

## 📋 Обзор

YRIKub - это многофункциональная платформа для Telegram с двойной архитектурой. Она сочетает в себе возможности UserBot на базе Gram.js и стандартного бота на базе Telegraf. Вся система написана на TypeScript и имеет модульную структуру для легкого расширения функционала.

Проект разработан с учетом производительности и удобства использования. Каждый из компонентов (UserBot и обычный бот) работает независимо, но в рамках единой экосистемы.

## 🌟 Возможности

### 🤖 UserBot

- **Модульная система** - Легко расширяемая архитектура, все модули находятся в `src/user/modules`
- **Системный мониторинг** - Команда `.menu` выводит детальную информацию о системе:
  - Данные о процессоре (CPU)
  - Информация о памяти (RAM)
  - Сведения о видеокарте (GPU)
  - Сетевой пинг до Telegram и Google
  - Данные пользователя
- **Динамическое управление** - Команда `.module` позволяет включать/отключать функциональность на лету без перезапуска
- **Асинхронная обработка** - Все операции выполняются асинхронно для максимальной производительности
- **Интеграция с ботом** - Взаимодействие между UserBot и стандартным ботом через команду `.tsi`

### 🤖 Стандартный бот

- **Базовый модуль** - Модуль `core` обеспечивает выполнение основных команд (/start, /help)
- **Интерактивные функции** - Модуль `inline-buttons` обрабатывает интерактивные кнопки и инлайн-запросы
- **Случайные мудрости** - Функция для отображения случайных мотивационных фраз
- **Модульная архитектура** - Аналогичная UserBot'у система модулей в `src/bot/modules`

## ⚙️ Архитектура

YRIKub имеет чётко структурированную архитектуру:

```
src/
├── bot/               # Стандартный бот (Telegraf)
│   ├── main.ts       # Точка входа для бота
│   ├── modules/      # Модули бота
│   │   ├── core/     # Базовые функции
│   │   └── inline-buttons/ # Обработка инлайн-кнопок
│   └── types.ts      # Типы для бота
│
├── user/              # UserBot (Gram.js)
│   ├── main.ts       # Точка входа для UserBot
│   ├── modules/      # Модули UserBot
│   │   ├── core-commands/ # Управление модулями
│   │   ├── menu/     # Системная информация
│   │   └── tsi/      # Интеграция с ботом
│   ├── types.ts      # Типы для UserBot
│   └── utils/        # Вспомогательные функции
│
└── main.ts           # Общая точка входа
```

## 🚀 Установка

1. **Клонирование репозитория:**
   ```bash
   git clone https://github.com/derxanax/YRIKub.git
   cd YRIKub
   ```

2. **Установка зависимостей:**
   ```bash
   npm install
   ```

## 🔧 Настройка

1. **Настройка окружения:**
   - Создайте файл `.env` на основе `.env.example`
   - Заполните следующие параметры:

   | Параметр | Описание | Где получить |
   |----------|----------|-------------|
   | `API_ID` | ID приложения Telegram | [my.telegram.org](https://my.telegram.org) |
   | `API_HASH` | Хеш приложения Telegram | [my.telegram.org](https://my.telegram.org) |
   | `SESSION_STRING` | Строка сессии для UserBot | Генерируется при первом запуске |
   | `BOT_TOKEN` | Токен стандартного бота | От [@BotFather](https://t.me/BotFather) |
   | `BOT_USERNAME` | Имя пользователя бота | Настраивается в @BotFather |

## ▶️ Запуск

### Режим разработки
Запуск с автоматическим перезапуском при изменении файлов:
```bash
npm run dev
```

### Сборка и запуск в продакшн
```bash
npm run build
node dist/main.js
```

## 📝 Команды

### Команды UserBot:
| Команда | Описание | Примечания |
|---------|----------|------------|
| `.menu` | Показывает информацию о системе | Отображает CPU, RAM, GPU, пинг и данные пользователя |
| `.module` | Показывает список модулей | Отображает состояние всех модулей (вкл/выкл) |
| `.module on <модуль>` | Включает указанный модуль | Например: `.module on menu` |
| `.module off <модуль>` | Выключает указанный модуль | Например: `.module off menu` |
| `.module info <модуль>` | Показывает информацию о модуле | Например: `.module info menu` |
| `.tsi` | Запускает инлайн-тест бота | Требует настройки `BOT_USERNAME` |

### Команды стандартного бота:
| Команда | Описание |
|---------|----------|
| `/start` | Начало работы с ботом |
| `/help` | Список доступных команд |
| `/test` | Проверка работы инлайн-кнопок |

## 📂 Модули

### Модули UserBot:
1. **core-commands** - Управление модулями (включение, выключение, информация)
2. **menu** - Отображение системной информации и данных пользователя
3. **tsi** - Интеграция со стандартным ботом через инлайн-режим

### Модули стандартного бота:
1. **core** - Базовый модуль с командами /start и /help
2. **inline-buttons** - Модуль для работы с инлайн-кнопками и случайными мудростями

## 🛠️ Разработка

### Создание новых модулей

Для создания нового модуля UserBot:
1. Создайте папку в `src/user/modules/<имя_модуля>`
2. Добавьте файлы:
   - `index.ts` - Код модуля
   - `info.json` - Информация о модуле

Пример структуры `info.json`:
```json
{
  "name": "название-модуля",
  "version": "1.0.0",
  "description": "Описание модуля",
  "author": "Ваше имя",
  "enabled": true
}
```

Пример структуры `index.ts`:
```typescript
import { Module } from "@/user/types";
import { TelegramClient } from "telegram";
import { NewMessageEvent } from "telegram/events";
import info from "./info.json";

async function commandHandler(client: TelegramClient, event: NewMessageEvent, args: string[]): Promise<void> {
  // Ваш код здесь
}

export const yourModule: Module = {
  info: info,
  handler: commandHandler,
  commands: ["команда1", "команда2"],
};
```
